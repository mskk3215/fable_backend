# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore all logfiles and tempfiles.
/log/*
*.log
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore uploaded files in development.
/storage/*
/storage/.keep
/tmp/storage/*
/tmp/storage/
/tmp/storage/.keep


# Ignore master key for decrypting credentials and more.
/config/master.key

/public/uploads

.env

### Terraform関連の設定 ###

# ローカルの.terraformディレクトリを無視
**/.terraform/*

# .tfstate ファイルを無視
*.tfstate
*.tfstate.*

# クラッシュログファイルを無視
crash.log
crash.*.log

# 機密情報が含まれる可能性がある.tfvarsファイルを無視
*.tfvars
*.tfvars.json

# リソースをローカルでオーバーライドするために使用されるoverrideファイルを無視
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# バージョン管理に含めたいオーバーライドファイルがある場合は、否定パターンを使用
# !example_override.tf

# terraform plan コマンドの出力ファイル tfplan を無視
# 例: *tfplan*

# CLI構成ファイルを無視
.terraformrc
terraform.rc

# キーペアファイルを無視
*keypair*

# 圧縮ファイルを無視
*.gz
*.zip
